---
phase: 01-game-engine
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/SoundManager.js
  - index.html
  - tests/engine.test.js
autonomous: true
requirements:
  - SND-01

must_haves:
  truths:
    - "SoundManager exports a class with no-op stub methods for: cardDealt, betPlaced, chipsWon, roundWon, bust"
    - "SoundManager has zero side effects — all methods are empty functions"
    - "index.html exists as a minimal entry point that loads the engine module"
    - "All Phase 1 tests pass including SoundManager validation"
  artifacts:
    - path: "src/SoundManager.js"
      provides: "No-op sound stub module"
      exports: ["SoundManager"]
    - path: "index.html"
      provides: "Minimal browser entry point"
      contains: "<script type=\"module\""
  key_links:
    - from: "tests/engine.test.js"
      to: "src/SoundManager.js"
      via: "ES module import"
      pattern: "import.*SoundManager.*from.*SoundManager\\.js"
---

<objective>
Create the SoundManager no-op stub module and the minimal index.html entry point. Verify the complete Phase 1 engine works end-to-end.

Purpose: SND-01 requires sound stubs from day one so Phase 3 can wire them without structural changes. index.html provides the browser entry point that Phase 2 will build upon.
Output: SoundManager.js with stub methods, index.html, and all Phase 1 tests passing.
</objective>

<execution_context>
@C:/Users/mattf/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mattf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-game-engine/01-CONTEXT.md
@.planning/phases/01-game-engine/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 — GameEngine contract -->

From src/engine/GameEngine.js:
```javascript
export class GameEngine {
  constructor()                    // Initializes with 100000 cents, phase='BETTING'
  placeBet(amountCents)            // Returns state snapshot
  deal()                           // Returns state snapshot
  hit()                            // Returns state snapshot
  stand()                          // Returns state snapshot
  doubleDown()                     // Returns state snapshot
  startNewRound()                  // Returns state snapshot
  resetSession()                   // Returns state snapshot
  getAvailableActions()            // Returns string[]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SoundManager stub and index.html, add final validation tests</name>
  <files>src/SoundManager.js, index.html, tests/engine.test.js</files>
  <read_first>
    src/engine/GameEngine.js
    src/engine/Deck.js
    src/engine/Hand.js
    tests/engine.test.js
    .planning/phases/01-game-engine/01-CONTEXT.md
  </read_first>
  <action>
    1. Create `src/SoundManager.js`:
    ```javascript
    /**
     * SoundManager — no-op stub for sound events.
     * Phase 3 will wire these to AnimationManager lifecycle events.
     * To activate a sound: provide an audio file path and replace the no-op body.
     */
    export class SoundManager {
      /** Called when a card is dealt to any hand */
      cardDealt() {}

      /** Called when player places a bet */
      betPlaced() {}

      /** Called when player wins chips */
      chipsWon() {}

      /** Called when player wins a round */
      roundWon() {}

      /** Called when a hand busts */
      bust() {}
    }
    ```
    - Exactly 5 no-op methods: `cardDealt`, `betPlaced`, `chipsWon`, `roundWon`, `bust`
    - Each method is an empty function body `{}`
    - No DOM access, no imports, no side effects
    - JSDoc comments on each method describing when it fires

    2. Create `index.html`:
    ```html
    <!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>Blackjack</title>
    </head>
    <body>
      <h1>Blackjack</h1>
      <p>Game engine loaded. UI coming in Phase 2.</p>
      <script type="module">
        import { GameEngine } from './src/engine/GameEngine.js';
        import { SoundManager } from './src/SoundManager.js';

        // Verify modules load correctly
        const engine = new GameEngine();
        const sound = new SoundManager();
        console.log('GameEngine initialized:', engine.placeBet ? 'OK' : 'FAIL');
        console.log('SoundManager initialized:', sound.cardDealt ? 'OK' : 'FAIL');
      </script>
    </body>
    </html>
    ```

    3. Add SoundManager tests to `tests/engine.test.js`:
    - Import: `import { SoundManager } from '../src/SoundManager.js';`
    - Test: `new SoundManager()` creates an instance without errors
    - Test: `soundManager.cardDealt()` returns undefined (no-op)
    - Test: `soundManager.betPlaced()` returns undefined (no-op)
    - Test: `soundManager.chipsWon()` returns undefined (no-op)
    - Test: `soundManager.roundWon()` returns undefined (no-op)
    - Test: `soundManager.bust()` returns undefined (no-op)
    - Test: SoundManager has exactly these 5 methods as own prototype methods

    4. Add final integration validation tests to `tests/engine.test.js`:
    - Test: Complete round flow — placeBet(1000) -> deal() -> stand() -> verify ROUND_OVER + chips changed correctly
    - Test: Multiple rounds — play 3 consecutive rounds, verify chips accumulate correctly
    - Test: Session end — drain chips to 0, verify getAvailableActions returns ['resetSession']
    - Test: CODE-06 verification — `typeof document === 'undefined'` is true in Node.js test context (engine runs without DOM)

    5. Run full test suite: `node tests/engine.test.js`
  </action>
  <verify>
    <automated>cd C:/Users/mattf/Desktop/plans/Blackjack && node tests/engine.test.js</automated>
  </verify>
  <acceptance_criteria>
    - src/SoundManager.js contains `export class SoundManager`
    - src/SoundManager.js contains methods: `cardDealt()`, `betPlaced()`, `chipsWon()`, `roundWon()`, `bust()`
    - src/SoundManager.js does NOT contain `document` or `window`
    - src/SoundManager.js does NOT contain `import` statements (no dependencies)
    - index.html contains `<script type="module">`
    - index.html contains `import { GameEngine } from './src/engine/GameEngine.js'`
    - index.html contains `import { SoundManager } from './src/SoundManager.js'`
    - tests/engine.test.js contains `import { SoundManager }` from SoundManager.js
    - `node tests/engine.test.js` exits with code 0 and output contains "0 failed"
  </acceptance_criteria>
  <done>SoundManager has 5 no-op stubs, index.html loads engine modules, all Phase 1 tests pass, engine runs without DOM</done>
</task>

</tasks>

<verification>
- `node tests/engine.test.js` exits with code 0 and reports 0 failures
- `grep -r "document\|window" src/` returns zero matches in engine files (src/engine/*.js and src/SoundManager.js)
- All source files use `export` keyword (ES Modules, CODE-05)
- index.html uses `<script type="module">` (CODE-05)
- SoundManager has exactly 5 stub methods with empty bodies
- Complete round flow test passes: bet -> deal -> action -> payout
</verification>

<success_criteria>
- SoundManager.js exports a class with cardDealt, betPlaced, chipsWon, roundWon, bust as no-op methods
- index.html loads GameEngine and SoundManager as ES modules
- Full Phase 1 test suite passes with 0 failures
- No source file in src/ references document or window
- All files use ES module export/import syntax
</success_criteria>

<output>
After completion, create `.planning/phases/01-game-engine/01-03-SUMMARY.md`
</output>
